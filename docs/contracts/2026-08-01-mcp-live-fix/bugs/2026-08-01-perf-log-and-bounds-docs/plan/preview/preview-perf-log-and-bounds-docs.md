# DESIGN PREVIEW — Perf Log & Bounds Docs
```mermaid
flowchart LR
    A[per-call INFO logs] --> B[DEBUG level]
    C[list cap 100 / store_memory seq / export 10k] --> D[Accepted decisions doc section]
```
- Log-level reclassification + explicit docs section.
