//! Metrics collector for telemetry subsystem.
//!
//! (Stub — Phase 2)

/// Collects and aggregates runtime metrics.
#[allow(dead_code)]
pub struct MetricsCollector {
    // TODO(phase2): add metric registry, counters, histograms
}

#[cfg(test)]
mod tests {
    use super::*;

    /// Placeholder test for Phase 2 metrics collection.
    #[test]
    fn test_placeholder() {
        let _collector = MetricsCollector {};
    }
}
